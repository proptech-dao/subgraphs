{
  "name": "hct-testnet",
  "license": "UNLICENSED",
  "scripts": {
    "auth": "graph auth",
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --product hosted-service propyech-company/holiday-tokens ",
    "create-local": "graph create --node http://localhost:8020/ propytech-company/hct-testnet",
    "remove-local": "graph remove --node http://localhost:8020/ propytech-company/hct-testnet",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 propytech-company/hct-testnet",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.52.0",
    "@graphprotocol/graph-ts": "0.30.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
